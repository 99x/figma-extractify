# Security Audit — ESAA-Security Pipeline

Runs a full 4-phase security audit of the figma-extractify project using the ESAA-Security framework (16 domains, 95 checks). Produces a numbered audit directory under `security-audits/` at the monorepo root.

---

Use the `extractify-security-audit` skill to execute this command.

The arguments are: $ARGUMENTS
