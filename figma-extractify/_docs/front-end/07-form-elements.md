## Figma page contract: Form Elements

This doc defines the **required structure** of the Figma page named **`Form Elements`** and the exact repo updates for form-related components.

### Figma node (project-specific)

> The Figma URL for this step is managed in `_docs/figma-paths.yaml` under `setup.form-elements`.
> Edit that file directly, or run `/extractify-setup` and the wizard will ask you for it.

### Source of truth

- **Figma**: the `Form Elements` page (inputs, selects, checkboxes, radios, textareas, labels, error states, etc.)
- **Repo**: `src/components/` (one component per element type) + `src/assets/css/base/forms.pcss`

---

## Status

> ⚠️ **This doc is a placeholder.** Detailed component contracts will be added here once the form elements are defined. Run `/extractify-setup` and skip this step until the doc is complete.

Components to be defined (details TBD):

- `Input` — text, email, password, number, tel, url
- `Textarea`
- `Select`
- `Checkbox`
- `Radio`
- `Label`
- `FormField` — wrapper combining label + input + error message
- `FormError` — inline error message

---

## Extraction checklist (placeholder)

- [ ] _(To be filled in once component contracts are defined)_
